<div class="position-absolute top-50 start-50 translate-middle">
  <%= render EventsWeb.EntryView, "details.html", conn: @conn, entry: @entry %>
  <h3 class="display-6 my-4">You've been invited!</h3>
  <%= render "resp_form.html", 
    Map.put(
      assigns, 
      :action, 
      Routes.entry_invitation_path(@conn, :update, @entry.id, @invitation.id)) %>
</div>
